// Criando as variáveis
let estudante = true;
let cupom = false;

// Verificando se o desconto foi liberado
if (estudante || cupom) {
    console.log("Desconto liberado!");
} else {
    console.log("Desconto não liberado!");
}